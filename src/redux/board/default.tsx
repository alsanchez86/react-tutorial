// BoardState
export const defaultCells: string[][] = Array(3).fill(Array(3).fill(""));
export const defaultWinner: number[] = [];
export const defaultHistory: string[][][] = [[[]]];
export const defaultDraw: boolean = false;
export const defaultStep: number = 0;
export const defaultXIsNext: boolean = false;
// Action
export const defaultType: string = "";
export const defaultValue: object = {};